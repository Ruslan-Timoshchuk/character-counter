# Character Counter

A Java utility library for counting the frequency of characters in a string  

The project supports full character-frequency analysis, maintains order of appearance,  
and provides an optional caching layer to avoid recalculating results for repeated inputs

🚀 Features

Counts occurrences of all characters: letters, numbers, symbols, whitespace, etc.

Returns results in a LinkedHashMap to preserve the order of first appearance

Includes a CachedCharCounter wrapper that memoizes results for repeated inputs

Lightweight and easy to integrate

Fully tested with JUnit and Mockito